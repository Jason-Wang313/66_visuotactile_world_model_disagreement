# Plan

Build paper 66 `visuotactile_world_model_disagreement` from the shared pool, compile PDF to Downloads only, and publish the exact-name public repo.

2026-06-15 continuation plan:

1. Re-audit the real MuJoCo visuotactile-disagreement evidence before changing the terminal decision.
2. Verify code compilation, CSV integrity, baselines, ablations, stress sweeps, uncertainty, PDF rebuild, Downloads-only artifact placement, and public GitHub availability.
3. Keep `KILL_ARCHIVE` unless the branch-preserving disagreement planner clearly beats mean fusion, ensemble uncertainty, diagnostic probing, and no-mechanism ablations.
4. Preserve the paper as a negative result because the current evidence fails that gate.
